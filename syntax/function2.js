console.log(Math.round(1.6)); // 2
console.log(Math.round(1.4)); // 1  반올림 함수


function sum(first, second){ // parameter(매개변수) 함수 선언 위치 상관없음
    return first+second; 
}

console.log(sum(2,4)); // argument(인자)